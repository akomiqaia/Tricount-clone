import React from "react";

export const Form = props => {
  return (
    <div>
      {/* hopefully i can manage to put all the form material here and later render it on the main App */}
    </div>
  )
}

export default Form